/**
 * Ambiente de PRODUÇÃO — usado por padrão no `ng build`.
 *
 * TODO(deploy): depois de criar o serviço da API no Render, troque a URL
 * abaixo pela URL pública dele (mantenha o sufixo /api).
 * Ex.: https://catalogo-vinho-api.onrender.com/api
 */
export const environment = {
    production: true,
    apiUrl: 'https://SUA-API.onrender.com/api',
};
